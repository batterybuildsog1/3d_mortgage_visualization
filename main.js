// Import global styles
import './styles.css';
import './css/ai-chat.css';
// Note: dti-calculator.css is likely loaded dynamically or within its own scope,
// so we might not need to import it globally here.

// Import the main application logic
import './js/script-new.js';

// Import the visualization module to define global functions like window.updateVisualization
import './js/visualization/index.js'; 

console.log('Vite main entry point loaded.');
